task main ()
{
	SetSensorLight(IN_1);
	SetSensorLight(IN_2);
	SetSensorLight(IN_3);
	SetSensorLowspeed(IN_4);
	OnFwd(OUT_BC, 100);

	while (1==1)
	{
		NumOut(0,0,LCD_LINE1, Sensor(IN_1));
		NumOut(0,10,LCD_LINE2, Sensor(IN_2));
		NumOut(0,20,LCD_LINE3, Sensor(IN_3));
		NumOut(0,30,LCD_LINE4, SensorUS(IN_4));

		if(SensorUS(IN_4)<10)
		{
			OnFwd(OUT_BC, 30)
		}
	}
}
